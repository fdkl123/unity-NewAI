[project]
name = "unity-ai-orchestrator"
version = "0.1.0"
description = "AI Unity Project Bootstrapper and performance-oriented orchestration tools."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
unity-ai = "bootstrapper.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
